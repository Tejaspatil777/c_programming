#include<stdio.h>
#include<stdlib.h>
#include<string.h>
#include<math.h>
/*Problem Description
A text processing application needs a system to perform various string
manipulation tasks such as formatting, searching, and analyzing text
data. This case study outlines a structured approach for developing a
solution using string operations and functions in C.Tasks
1. String Operations
• Implement the following operations using character arrays:
• Input and display a string
• Count the number of vowels and consonants
• Reverse the string
• Convert the string to uppercase and lowercase
2. Functions
• Implement the above operations using separate functions:
• void inputString(char str[])
• void displayString(char str[])
• int countVowels(char str[])
• int countConsonants(char str[])
• void reverseString(char str[])
• void toUpperCase(char str[])
• void toLowerCase(char str[])Solution Design
• Use functions to modularize each string operation.
• Use loops to iterate through characters in the string.
• Use character handling functions from <ctype.h> like toupper()
and tolower().
• Use gets() or fgets() for string input (with caution).
• Use strlen() to determine string length.
• Use a loop and isalpha(), isupper(), islower() to classify
characters.
• Use a temporary variable to swap characters for reversing.*/
int main(){

return 0;
}
#include<stdio.h>
#include<stdlib.h>
#include<string.h>
#include<math.h>
/*Boolean Problem
Problem Description
Compute the following:
• Count of Set Bits (1's) in the Binary Representation: For each
integer in the sequence, calculate the number of set bits (i.e.,
how many '1' bits are there in its binary form).
• Identify Common Set Bits: From the sequence of integers,
identify which bits are set across all numbers. A "common set
bit" is a bit that is set (i.e., a '1') in all integers.
• Perform Bitwise XOR: Perform a bitwise XOR of all integers and
output the result. XOR-ing all numbers of results in a number that
represents the combined effect of all bitwise operations.
Input:
• The program will take an array of integers as command line
arguments.
Output:
• For each integer, output the number of set bits in its binary
representation.
• Output the common set bits (if any) across all integers.
• Output the result of XOR-ing all integers.*/
int main(){

return 0;
}
#include<stdio.h>
#include<stdlib.h>
#include<string.h>
#include<math.h>
/*Write a C program that accepts two integer values as command-line
arguments. Your task is to swap their values using only the bitwise XOR (^)
operator - do not use any temporary variables.
After swapping the values:
• Print the result to the console.
• Provide a clear explanation of how the XOR swap algorithm works,
including why it allows swapping without using additional memory.
• Input must be accepted via argv[] from the command line.
• Properly handle the case where incorrect or insufficient arguments
are passed.
• The XOR logic must be implemented correctly and clearly commented.
• Include a brief explanation as output or in comments describing the
bitwise operations.*/
int main(){

return 0;
}